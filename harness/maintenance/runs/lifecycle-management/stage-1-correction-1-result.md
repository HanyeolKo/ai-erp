# Stage 1 correction 1 result (contract revision 3)

Status: `ready-for-review`. This bounded correction addresses the independent review findings only; final verdict and parent acceptance remain external to the implementer.

The router and reviewer now declare the `planning` domain in the canonical spec and common role files while preserving access, model tiers, and capabilities. The increment lifecycle now defines planning-only completion after applicable independent `increment-plan-review` and parent acceptance, valid small-fix reuse, scoped amendment and re-review, downstream invalidation, and no fabricated implementation/release evidence.

The task rubric now routes general product planning to `increment-plan-review` and keeps screen decisions on `ui-plan-review`. The evaluation skill lists `product-planner` as an executor and its canonical/projection files are byte-identical. New focused mutations prove that removing assignment contract revision, removing assignment acceptance criteria, and changing the plan skill evaluator fail for their intended reasons. Existing guards were sufficient, so no unrelated verifier behavior was added.

Provider preflight ran after common changes and before the final projection sync. Exact command output and exits are recorded in `stage-1-correction-1-checks.json`; structural verification, all 35 regressions, UX smoke, diff check, and projection parity passed.

No stage 2/3 work, state/journal edit, parent contract edit, application change, release action, commit, push, or publish was performed. The original stage result remains historical and now points to this correction evidence.
