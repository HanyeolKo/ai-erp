# Parent-approved implementation contract: Spark implementation role

The user explicitly requires GPT-5.3-Codex-Spark to perform code implementation while the upper agent deeply analyzes and clearly decides the implementation scope and content. You are that Spark implementer. Implement this exact bounded contract. The parent retains design, review, remediation decisions, Git/PR/merge, and Notion delivery. This later explicit instruction supersedes older inherited-model-only repository text. Do not delegate implementation again.

Baseline: branch codex/ui-ux-agent-harness, HEAD 1905d3ff7f46115cb5012739483a4e06ebc7fe11. Preserve all unrelated files, original receipts/evidence, and byte-pinned vendor snapshots. No application code changes in this task. No Git mutations, external writes, browser actions, dependencies, or generated credential/config copying.

## Approved design

1. Keep schema 1.2/core/Codex, limits 4 and depth 2. Extend the purpose to cover upper-agent-directed Spark implementation in addition to mandatory UI planning. Add code domain for frontend/backend/scripts/infra/.github and root build/config; role text covers ALL code, tests, and behavior-affecting configuration regardless of path.
2. Existing router, designer, reviewer become abstract model_tier deep but continue inheriting native model AND effort (omit both keys). Router analyzes, constructs assignments and collects evidence; designer writes planning/design docs only, never UI code/tests; reviewer independently analyzes plans/diffs/evidence and issues verdicts. Parent owns final acceptance and merge, never delegates those to Spark.
3. Add implementer: execution lane; execution+verification capabilities ONLY, fast tier, workspace-write, domains code/harness/ui-ux. Canonical role and .codex/agents/ai-erp-implementer.toml thin wrapper. Set model exactly gpt-5.3-codex-spark and model_reasoning_effort high. Never grant verdict/routing/improvement. Cannot own evaluators, push, merge, publish, or re-delegate. It executes an explicit parent contract, writes allowed code/tests/config, runs checks, reports results/unknowns. Architecture, authorization, scope, UX, unclear requirements and failed assumptions return to parent for a revised contract. No silent model fallback or direct parent implementation if Spark is unavailable.
4. DAG: router->designer->reviewer->implementer for UI; router->implementer ONLY for non-UI after a complete upper-agent contract. No designer->implementer shortcut. Spark returns results to the invoking parent; do not encode this as a reverse DAG edge. Parent independently reviews results and sends bounded revised instructions via execution loop.
5. Add task-review rubric evaluator (runner router, owner reviewer) for implementation contracts/results. Entry ai-erp and ai-erp-eval use task-review; UI domain skills retain ui-plan-review. Add ai-erp-implement domain skill entering implementer and using task-review, canonical and identical .agents projection. Every UI change still needs real specialist plan and passing independent plan review. Do not mistakenly require designer for implementation-only code skills.
6. Add concise English IMPLEMENTATION-CONTRACT.md and IMPLEMENTATION-RESULT.md templates and TASK-REVIEW-RUBRIC.md. Contract: task/revision/base Git state, parent decision owner, objective, current behavior and source evidence, chosen design rationale, exact permitted files/symbols, preserved changes and exclusions, interfaces/data/permission/state rules, numbered observable acceptance criteria, validation commands/expected outcomes, UI flag and real plan/review evidence when applicable, no unresolved decisions, explicit parent ready-to-implement determination. Result: matching task/revision, selected runtime model and invocation evidence, ready-for-review or blocked (not approval), exact changed paths, acceptance/evidence mapping, actual check commands/exit codes/output paths, checks not run, deviations/uncertainties. Rubric requires parent contract + exact Spark invocation + diff/acceptance/test evidence + parent independent verdict. Distinguish static config checks from actual invocation/quality; no fake proofs.
7. Update AGENTS.md, HARNESS, TEAM-ARCHITECTURE, all affected role/skill files and projections, EXECUTION/EVAL loops, ENVIRONMENT, CONTEXT-BUDGET, RECOVERY to remove blanket model inheritance and backend implementation exceptions. Native thin wrappers stay exactly in factory format and model exceptions stay only for implementer. Keep English canonical docs under 100 lines; target 50.
8. Add an improve-mode delta-plan receipt under harness/maintenance/runs/add-spark-implementer, reference it in spec, preserve old create receipt. Current/recommended/selected core; updated purpose and Spark policy come from latest explicit request, retained choices from approved-decision. No fabricated user interviews. Run provider-path preflight after spec/receipt updates and BEFORE writing new provider projections. Append D-004 and journal agent-change/adapter-change/skill-change/evaluator-change, explaining supersession of old D-002 for the implementer only. Update state next action truthfully. Existing historical raw evidence stays unchanged.
9. Update Korean guide docs/architecture/ui-ux-agent-guide.md, README and existing Change Report to describe four roles and actual model split, upper deep decisions, mandatory contract and review, backend too, and CLI fallback example using supported codex exec -m gpt-5.3-codex-spark -c model_reasoning_effort=high --sandbox workspace-write. Mention parent keeps its model, exact selected model must be recorded, and no --dangerously-bypass flags. Parent will synchronize existing Notion archives and commit.

## Verification implementation

Update only scripts/verify-harness.py and scripts/test_verify_harness.py. Preserve factory validator bytes and compatibility shim; preserve vendor/tracking/sandbox/path checks. Required protections:
- Four declared roles, required graph including both implementation routes, forbidden UI shortcut/back edges. All UI domain skills must enter designer; ai-erp-implement must enter implementer. task-review owner reviewer and runner router, correct skill evaluator links.
- Spark wrapper exact model/high; nonimplementer model+effort keys still forbidden; root default model overrides still forbidden.
- Implementer execution+verification only, no evaluator ownership; upper model_tier deep and implementation model_tier fast. Required templates/rubric/role paths and entry references present.
- Add meaningful mutation tests for wrong/missing Spark model, wrong effort, upper override, UI review bypass, implementer verdict capability/evaluator ownership, implementation skill misrouting and missing contract template. Retain current 10 tests, adjust descriptions/expectations to the new legitimate model split. Do not add a generic contract execution framework or --contract/--result CLI to this task; actual contract completeness and runtime evidence are evaluated by the upper reviewer rubric.
- Update success output to truthful role-specific model policy. Python 3.11+ compatible; keep guarded onerror cleanup.

## Acceptance and checks

- All new/updated canonical and native projections agree, core graph acyclic, UI always specialist-reviewed, all code goes to exact Spark, final judgment stays upper.
- Run python -B scripts/verify-harness.py (not --require-tracked until parent stages new files), python -B scripts/test_verify_harness.py, python -B scripts/smoke-ux-skills.py, git diff --check. Fix only within this contract; if design cannot fit factory, report concrete issue to parent rather than weakening validation.
- Do not invent pass evidence. Write concise actual implementation report to tmp/spark-harness-result.md listing files, tests/results and blocked items. Parent records durable review/delivery evidence.

Authorization covers this implementation. No additional user design approval is needed. If .codex/.agents protected writes are unavailable, generate their exact intended files under tmp/spark-provider-output and report mapping so parent can mechanically copy your implementation with its authorized escalation. Do not change another path as a workaround to permissions.
