# Parent review requirements for final verification

1. The handoff DAG describes artifact/prerequisite order, not a nested spawn chain. Every role returns to the upper orchestrator, which sequentially invokes the next role. The read-only reviewer does not need to spawn a writable implementer. Preserve depth 2.
2. task-review applies actual implementation evidence (Spark invocation, diff, implementation tests) only if implementation is within the task. A planning-only task can complete on a passing ui-plan-review with implementation checks explicitly not applicable. Do not force code changes or fabricate Spark activity for a planning-only user request.
3. Code/config/test conflict resolution is still implementation: upper agent decides the resolution and performs Git operations, Spark applies the actual code changes.
4. D-004 should explicitly supersede D-001's three-role/balanced/backend ordinary-flow choices as well as D-002's blanket inheritance, preserving historical records.
5. RECOVERY and CHECKPOINT must recover the implementation contract revision, Spark invocation/result evidence, and upper review state when resuming code work.
6. Partial review of current rewritten files found missing mandatory contract content. implementer.md must explicitly forbid push/merge/publication/redelegation, require a complete parent contract with no unresolved design decisions, and return architecture/permission/UX/scope ambiguity to parent. All actual code implementation, including UI and new paths outside today's domain index, uses Spark. A domain label must not create a bypass.
7. TASK-REVIEW-RUBRIC currently calls implementer only a non-UI executor and omits exact model/invocation evidence. It must cover UI and non-UI implementation, require actual gpt-5.3-codex-spark selection plus invocation evidence, actual diff/acceptance mapping and parent-independent verdict. Static wrapper verification is not proof of actual invocation. Apply this conditionally for tasks containing code implementation, per item 2.
8. TEAM-ARCHITECTURE currently prints non-UI router -> implementer -> reviewer as if a normal DAG. Describe non-UI router -> implementer, return results to parent, then parent requests independent review. For UI specify router -> designer -> reviewer -> implementer, with parent controlling each transition and final review after the result return. Do not encode reverse edges.
9. IMPLEMENTATION-RESULT must require matching task/contract revision, selected runtime model/invocation evidence, changed paths, numbered acceptance/evidence mapping, command/exit/output paths, checks not run, deviations/uncertainties, and ready-for-review or blocked status; Spark never sets final approval.

These are the upper agent's accepted clarifications to tmp/spark-harness-contract.md. They do not change the mandatory Spark executor for any real code changes.

## Latest user instruction supersedes exact-Spark-only selection

The user subsequently said: "spark와 유사한 저급 모델배정하는 방향으로 기억해놔."
The lasting preference is to delegate implementation to Spark or a comparable lightweight/affordable model; upper agents keep deep analysis, explicit decisions/contracts, independent review and final acceptance. Current permitted implementer models are gpt-5.3-codex-spark (preferred/default) and gpt-5.6-luna (documented fallback when Spark has a limit or cannot be invoked). This user instruction authorizes using Luna now. Do not switch upper models. Record the actual selected implementation model and reason. Replace any exact-Spark-only prohibition in the original contract and above notes with this two-model implementation policy. The four-role layout stays unchanged: native implementer wrapper defaults to Spark/high, while an explicit per-invocation Luna/high override is allowed. A lightweight model never receives scope, architecture, permission, UX, final verdict or merge decision authority.
