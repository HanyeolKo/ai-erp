# Bounded correction

Task verification-proportionality; attempt 2, same Luna/high; parent /root. Read review-1.json. Correct only VP-01 through VP-04 and the explicit context-budget/evidence notes below. Preserve initial result.md/checks.json; return correction-result.md and correction-checks.json. No delegates or broader polishing.

- VP-01: replace or scope the actual contradictory clauses, not merely append a new policy paragraph. Low/standard TASK-RECORD (inline permitted) must satisfy parent contract/assignment/result with minimal fields. Scope mandatory detailed budgets/context/usage and separate files to high/complex assignments. Check HARNESS, EXECUTION-LOOP, DELEGATION, eval canonical/projection and other active references to ensure no unconditional rule blocks compact acceptance.
- VP-02: remove any-reassignment escalation requirement from eval canonical/projection and reviewer. Actual capability-based model escalation only; same-model correction, ordinary handoff/reassignment and same-cost alternative do not require MODEL-ESCALATION. Retain selection reason for real model alternatives without separate forms.
- VP-03: explicitly allow Sol/medium for mandatory normal UI/product-plan reviews in structured policy and reviewer/plan skill instructions as applicable. High-risk plan/release review remains Astra/high; specialist and independent plan gates stay required. Saved reviewer Astra/high default remains unchanged; generic explicit Sol/medium reviewer uses read-only role.
- VP-04: validate essential tier checks arrays and record low_standard_single_record/escalation_only fields (and the new normal-plan review selection) with small structured mutations. Do not add NLP/prose blacklist guards.
- Parent now explicitly allows harness/budget/CONTEXT-BUDGET.md to replace stale inherited-model/4-2 limits with concise references to current centralized model/risk policy and canonical2/1 limits. Initial out-of-scope edit remains recorded as deviation, not retroactively claimed preauthorized. Do not expand this file further.
- Correct the skipped-standalone explanation in correction evidence: .agents projections changed; project verification embeds Factory validation and parity, so a redundant standalone run is unnecessary. Do not alter old checks.json; annotate its skip explanation as superseded.
- Run only affected focused policy tests (existing6 plus new meaningful field/plan cases) and project structural/parity verifier + diff check. Reuse prior83 suite for unaffected coverage. Do not rerun full83 suite; guard edits must stay in the new proportionate-policy function/tests, so other pre-existing guard tests remain valid. Provider preflight only before actual projection updates, use current receipt. UX/search/runtime/tracking unchanged, skip.
- Capture direct raw output with actual command arrays/cwd/exit/stdout/stderr. Freeze and return <=250 words with exact counts and evidence; parent reuses same independent reviewer for these4 criteria only.
