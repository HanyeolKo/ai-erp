# Visual change plan

Map each screen to the accepted shared pattern contract. The parent persists this read-only proposal before independent review.

- Task and contract revision:
- Source pattern contract ID/version and acceptance evidence:
- Functional plan path and `ui-ux-designer` handoff evidence:
- If this is pattern-contract-only review, record screen mapping as `N/A` and defer this plan until acceptance.

| Screen/path | Shared rule IDs | Visual change and measurable before/after acceptance | Preserved behavior evidence | Responsive/keyboard checks | Exception or status |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

- Data-encoding layout values checked for semantic preservation:
- DOM order, focus, accessible names, visibility, and action placement checked:
- Checks run (commands, exit codes, output paths):
- Checks not run and reason:
- Independent `ui-plan-review` handoff:

Only presentation changes are in scope: spacing, alignment, placement, sizing, typography, color tokens, borders, surfaces, density, and responsive presentation. Route behavior-affecting layout changes back to the parent for a revised contract.
