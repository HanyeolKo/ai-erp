# Recovery

Restore context from `harness/HARNESS.md`, the spec, current state, and the latest journal entries.
If references, provider parity, or vendor hashes fail, stop dependent work and repair the canonical source first.
Reproject only affected provider artifacts after a successful provider-path preflight.
If the specialist cannot be invoked, disclose the runtime limitation and keep the screen-planning prerequisite unresolved.
If a screen plan fails review, return the review evidence to the orchestrator for a bounded specialist revision.
Record failures under a stable key such as `screen-plan:missing-permission-state`; after three attempts, report the exact blocker and next action.
Never erase failed evidence, weaken criteria, change the profile, or add an approval gate merely to hide a failure.
