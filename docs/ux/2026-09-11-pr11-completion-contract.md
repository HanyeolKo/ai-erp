# PR 11 completion contract

- Revision 1; owner /root; baseline 6b94445; status READY for the bounded CI repair below.
- Authorization: user requests completing their PR 11 additions and deployment. This supersedes the prior draft-only hold: commit/push, ready PR, merge after passing required checks/review, and the existing production deployment workflow are authorized. Do not send real Gmail messages, alter user calendars/Drive sharing, or print credentials during verification.
- Preserve user commits 66acc79 and 6b94445. UI decisions remain the previously approved project/Workspace plan; no new UI behavior is proposed by this repair.
- Routing: ai-erp; actual Spark/high implementation, upper-agent contract and independent reviewer verdict. Current Spark quota is available; do not reuse the earlier quota fallback without new evidence.
- Partition CI: backend/src/integrationTest/java/com/aierp/Phase1PersistenceIntegrationTest.java and a focused Calendar dispatch integration test if necessary. Repair the outdated synchronous retry expectation, not production behavior merely to satisfy the old test. Production retry enqueues PENDING; provider I/O occurs in CalendarDispatchWorker after local commit.
- Acceptance: preserve revision/acknowledgement/audit/publication/notification assertions. Assert retry is durably queued without inline provider calls. Independently exercise configured binding/credential and actual worker with a synthetic adapter failure, verify safe transient projection state and intact business transaction, no external Google request. Disable test scheduler races with bounded test configuration where necessary; use existing user/project fixtures or insert valid identity rows required by V8.
- Scope excludes changing production Calendar behavior and deleting/weakening the external-failure isolation assertion. Return any discovered production defect to root for a separately bounded assignment.
- Root owns shared Gradle execution using Java 25; workers provide source tests and diff checks, no simultaneous Gradle builds, no worker commits/push/deploy. Local Docker is unavailable; required PostgreSQL execution occurs in GitHub CI and may not be marked PASS before execution.
- Review existing remaining Workspace/Calendar/OAuth defects against user repairs; assign only concrete remaining blockers. Parent runs appropriate full tests, API generation, frontend checks, deployment contract and CI. Merge only after independent release review and required CI succeed on current head. Verify deployed release SHA and health using existing deployment workflow.
- Documentation: concise Korean reader report local-first; consolidate same-context unread Notion page only after verifying actual reading status. Previous Notion block-limit failure does not affect code verification.

## Revision 2: remaining Calendar audit defect

- Parent high-risk assignment to the same actual Spark/high executor. Source manifest: CalendarDispatchWorker.java, CalendarProjectionRepository.java, CalendarProjectionEntity.java, GoogleCalendarDispatchTest.java and a dedicated Calendar PostgreSQL test only if needed. Context budget 12k tokens, return <=500 words; reuse the bounded Calendar context. Provider usage unavailable, not zero. Actual Spark quota was 0% consumed at dispatch.
- Independent reviewer identifies GW-I11: an audit claim changes SYNCED to PENDING, then transient/credential failure or lease expiry forgets audit identity and can reopen the six-hour horizon forever.
- Preserve durable audit identity through claim, release, completion and expired-lease recovery. Preferred minimal design: keep a scheduled audit projection SYNCED while its lease is held and carry an audit boolean in the immutable Claim. A new business revision remains normal PENDING delivery. Failed audits retain their original reconcileUntil, schedule next audit at bounded cadence (30s) and stop after that original horizon; they never enter ordinary PENDING infinite retry or extend horizon. Avoid new schema unless existing state cannot express this safely; return such decision to root.
- Tests must cover provider transient failure crossing horizon, credential failure/release crossing horizon, expired audit lease, and a newer genuine schedule revision superseding audit. Preserve ordinary confirmed-delivery retries and cancellation reconciliation after an uncertain normal insert. No frontend/API contract changes.
- Latest main harness is merged; selected verification tier high, relevant integration checks plus independent review. These repairs are offline-contract-only with synthetic adapters; deployment/provider activation needs separate verified production setup.

## Revision 3: shared identity fixture collision

- Parent /root; status READY; actual Spark/high executor; bounded manifest: Phase1PersistenceIntegrationTest.java only, test-only repair, return <=200 words, no Gradle/commit/push/deploy or child delegation.
- CI 34546087462 on 9cf04135 ran 34 PostgreSQL integration tests: Calendar isolation passed; only sharePermissionsExpiryRotationRevokeAndProfileReadBoundsAreEnforced failed at line 228 because the common fixture now already inserts the same identity primary key.
- Replace that test-local duplicate INSERT with an UPDATE of the existing fixture user's email/display name by id. Preserve the common valid identity row and all name-fallback, invitation permission, expiry, rotation, revoke and profile assertions. Do not use ON CONFLICT or remove assertions to conceal setup errors.
- Root checks the exact diff and runs required same-head CI; independent reviewer confirms the fixture repair. No application behavior changes, extra tests or broad retesting beyond affected compilation and required CI.
