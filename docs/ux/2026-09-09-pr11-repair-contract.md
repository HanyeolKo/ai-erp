# PR 11 repair contract

- Revision: 1; owner: /root; base: 2242c48756d4e77603c55baed7a10d85a6b11ae0.
- Authorization: user requested repair of identified incomplete work and commit in this environment. Commit/push to the existing PR branch; no merge, deployment, real Google writes, or history rewrite.
- Routing: ai-erp-implement; actual gpt-5.6-luna/high workers because Spark is unavailable in this environment. Independent reviewer follows implementation. Existing Workspace contract remains authoritative. No screen design changes; UI prerequisite is N/A for backend repair preserving existing DTOs and flows.
- Objective: repair confirmed CI fixture mismatch, Gmail claim/receipt races, Workspace transport/errors, Drive freshness/save permissions, Calendar temporary classification; verify existing OAuth/audit changes and V8 compatibility.
- Current evidence: deployment_contract.py expects the wrong missing-v8 message; assigned-ID JPA merge in Gmail claim; unlocked receipt transitions; unbounded body read; Drive check/save gap; Calendar maps temporary credential failures to permission.
- Design: atomic insert-only Gmail claim, transactional serialized terminal transitions and fresh grant barriers; bounded complete HTTP exchange and safe provider error taxonomy; short project-locked Drive save and grant revalidation; preserve transient Calendar retry semantics. Preserve existing API payloads and explicit-send behavior.
- Partition A permitted files: GmailService.java, MailSendRequestEntity.java, MailSendRequestRepository.java, identity/api/GoogleAccess.java, identity/api/GoogleAuthorizationService.java, identity/GoogleAuthorizationRepository.java; dedicated Gmail unit/integration tests and existing GoogleWorkspaceCoreTest fixture updates. Add a transactional named-API credential barrier if needed; coordinate interface with B. No other files.
- Partition B permitted files: GoogleHttpClient.java, DriveService.java, DriveReferenceRepository.java; new helper files only in googleworkspace; dedicated HTTP/Drive tests and Drive integration tests. Coordinate with A for GoogleAccess barrier, do not edit A files. Shared error helper may be consumed by Gmail via A.
- Partition C permitted files: CalendarDispatchWorker.java and calendar repositories/entities if needed to fix demonstrated audit failures; GoogleCalendar*Test.java; identity/GoogleOAuthFlowContextTest.java; scripts/tests/deployment_contract.py; dedicated V8 migration integration test. No A/B files.
- Workers may inspect all repository files. Only parent runs shared Gradle tasks. Workers may run isolated static checks and must return exact changed files, test commands, limitations and evidence. No worker commits or external writes.
- Required acceptance: (1) missing V8 rejects with correct diagnostic; (2) same-key concurrent Gmail calls produce at most one provider POST and mismatch cannot overwrite receipt; (3) finish/age-out cannot regress terminal state and stale grant cannot commit success; (4) stalled/oversized body fails within bound using real production transport with injectable local test endpoint; (5) provider failures map to safe distinct categories; (6) Drive disconnect/demotion before save prevents stale attachment and duplicate attachment replays; (7) Calendar temporary errors remain retryable and cancellation audit has a bounded horizon; (8) OAuth regression fixture and final backend/frontend checks run; (9) actual PostgreSQL concurrency and V7-to-V8 migration tested when runtime is available, otherwise explicitly pending.
- Validation: deployment-contract.sh; Gradle test integrationTest openapi3 bootJar with Java 25; pnpm api:generate frontend:test frontend:typecheck frontend:build; git diff --check. Docker build when Docker is available. Never label skipped checks PASS. Add targeted regression evidence, not implementation-mirroring tests.
- Parent ready-to-implement: ready. Runtime availability is a validation limitation, not permission to relax behavior or change toolchain versions.

## Revision 2 scope clarifications

- C may add regression tests in frontend/src/google-workspace.test.tsx for existing accepted denial/private-dialog and keyboard contracts; no screen implementation or design changes. Existing 2026-09-07 Workspace UI contract/review supplies intended behavior.
- B may extend platform/web/ExternalServiceFailure.java safe-code allowlist only if required; prefer existing codes.
- A also repairs the existing invalid CONNECTED enum case in GoogleAccess.status(Category), because Category has no such constant.
- Validation environment: local Java 17 only; Gradle 9.2.0 distribution resolution failed with UnknownHostException; Docker absent. Existing GitHub CI is the Java 25/PostgreSQL verification environment after the authorized commit/push. Local Vitest ran 207/207 successfully; typecheck cannot complete before backend-generated API types exist. No production deployment is authorized.

## Repair verification before commit

- Implemented partitions A/B/C with the declared Luna/high fallback and an independent read-only source review. Review findings about timeout cleanup, receipt failure state, concrete Spring mocks, checked exceptions, and the Drive test's membership lock protocol were corrected.
- Frontend final default-worker run: 14 files, 209 tests passed. Google/recovery paired run: 27 passed. Some earlier runs exposed intermittent existing navigation/fake-timer failures; isolated recovery and final default-worker runs passed. Production frontend code is unchanged.
- Harness schema verification passed; harness regression tests 22/22 passed; UI/UX offline smoke passed; git diff --check passed.
- Added real PostgreSQL service/repository tests for Gmail claim/replay/age-out, actual authorization account/grant barrier, Drive persistence and membership locking, and V7-to-V8 migration. These are not yet runtime-verified locally.
- Local deployment contract execution was blocked by its nested fake-deployment lock check (`backup: another deployment holds the lock`). Missing-V8 diagnostic expectation was corrected; full contract requires CI confirmation.
- Java 25 compilation, backend unit/integration tests, generated API/typecheck/build, full deployment contract and Docker image build remain pending the existing PR CI. Source review alone is not a merge/deployment PASS.
