# Latest UI implementation contract

- Task/increment: ui-workspace-refactor; base origin/main 058f782; plan latest-r2; implementation latest-css-r1; owner /root.
- Assignment: docs/ux/latest-ui-refactor-assignment.md. Functional plan and visual contract are docs/ux/latest-ui-refactor-functional-plan.md and latest-ui-refactor-visual-contract.md. Independent ui-plan-review is required before dispatch. Parent adoption of ERP-WORKSPACE-01/v2 follows its pass.
- Objective: consistently refine the presentation of every current route while preserving current project/Google functionality and existing topbar/sidebar/mobile shell.
- Evidence: baseline 19 routes at1440/390 in docs/ux/latest-ui-evidence; current CSS mixes repeated palette/borders/spacing, controls and semantic surfaces. Focused remedy: shared tokens and existing-selector CSS normalization. No behavior bug is being fixed.
- Exact product files: frontend/src/app.css, frontend/src/screens/schedules.css, frontend/src/screens/GoogleWorkspace.css. No TSX or tests/config/backend/dependencies edits. Existing selectors only; no wrappers or new controls. Preserve root16px, effective260px day canvas and data-encoding coordinates, breakpoint semantics and all existing visibility conditions. Avoid duplicate overriding rule piles; normalize current declarations in their appropriate owner while preserving cascade effects.
- Exclusions: source logic, handlers, hooks, API/generated contract, permissions, sessions, labels/content/order, ARIA, focus logic, calendar data geometry, latest workflows/policies, root worktree's existing rebase and user changes. Other worker owns additive harness changes; do not revert them.
- External mode: offline-contract-only, loopback synthetic fixtures on127.0.0.1:8080 proxied by5175, no external state or messages. Real Google integration is unchanged and not certified by this work. GitHub auth/fetch ready2026-09-11; publication requires independent review and same-SHA CI. No manual production deployment.
- Verification: existing232 frontend tests passed at latestbase before styling; current backend135 tests and OpenAPI generation passed; no new mirrored CSS tests warranted. After CSS batch run pnpm --dir frontend test, typecheck, build; git diff --check. Capture stdout/stderr/exit/cwd to docs/ux/latest-ui-evidence/*.txt and implementation result. Parent browser compares current before/after screenshots/control inventory and calendar coordinates, fullwidth/mobile/keyboard/dialog states. Existing integration tests cover unchanged data flows; browser fixtures certify local presentation only.
- Observable required acceptance: (1) exact3CSS product diff, no source behavior diff; (2) coherent ERP-WORKSPACE-01/v2 shared rules across all mapped routes/dialogs; (3) latest shell/mobile action visibility and order retained; (4) ordinary document overflow<=1px at1440/768/390/375/320CSS widths and200% equivalent reflow; (5) root16, day260 and event data positions unchanged; (6) visiblefocus and contrast4.5 text/3 boundary; (7) existing frontend tests/typecheck/build pass; (8) independent high task-review pass before release.
- Runtime mapping: CSS implementation executor Luna/high, explicit generic implementer dispatch; no self-verdict, no Git publication, no delegation. This is current repository default, no escalation. Context manifest: assignment, functional+visual+review docs, three CSS files, relevant TSX read-only. Fresh bounded fork none; minimum relevant file context, result output<=2500 tokens, usage/cache null.
- Results: docs/ux/latest-ui-refactor-implementation-result.md. Every acceptance item maps actual diff and evidence; unrun checks explicitly marked and returned to parent. Reviewer receives exact revision/diff after parent browser evidence. After correction rerun only affected checks; no unrelated retest loop.
- Checks not run before dispatch: post-change checks and browser after comparison.
- Parent ready-to-implement: blocked pending independent ui-plan-review pass and explicit parent acceptance appended below. No product edits until ready.
- Release linkage: user explicitly authorized Korean PR and merge. Exact reviewed SHA, CI and release readiness required; existing main workflow may automatically deploy. Release preparation/outcome are separate from any deployment claim.

Parent adoption: ERP-WORKSPACE-01/v2 shared pattern and identical screen mapping accepted after independent pattern-stage pass2026-09-11. Ready after reviewer records bounded screen-stage closure; no CSS edits before that closure.


Parent ready-to-implement: READY. Independent ui-plan-review pattern and mapped-screen stages passed (latest_review, Astra/high,2026-09-11);0stabledefects. Exact3CSS implementation now authorized under latest-css-r1 and parent-accepted ERP-WORKSPACE-01/v2. No pending decisions.

